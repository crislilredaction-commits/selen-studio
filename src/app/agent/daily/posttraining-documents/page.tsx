"use client";
import { useCallback,useEffect,useMemo,useState } from "react";
type DocumentRow={id:string;document_type:string;status:string;version:number;created_at:string;metadata?:Record<string,unknown>|null;organisations?:{name?:string|null}|null};
const labels:Record<string,string>={attendance_summary:"Relevé des présences",completion_certificate:"Certificat de réalisation",learning_assessment_evidence:"Preuve d’évaluation des acquis"};
export default function Page(){const[documents,setDocuments]=useState<DocumentRow[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(""),[message,setMessage]=useState(""),[filter,setFilter]=useState("attention");const load=useCallback(async()=>{setLoading(true);try{const r=await fetch("/agent/api/daily/posttraining-documents",{cache:"no-store"});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error??"Chargement impossible.");setDocuments(d.documents??[]);}catch(e){setError(e instanceof Error?e.message:"Chargement impossible.");}finally{setLoading(false);}},[]);useEffect(()=>{void load();},[load]);const visible=useMemo(()=>documents.filter(d=>filter==="all"||["to_check","to_validate","correction_requested"].includes(d.status)),[documents,filter]);async function review(id:string,action:"validate"|"request_correction"){const note=action==="request_correction"?window.prompt("Motif ou correction demandée :","")??"":"";const r=await fetch("/agent/api/daily/posttraining-documents",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id,action,note})});const d=await r.json().catch(()=>({}));if(!r.ok)return setError(d.error??"Action impossible.");setMessage(action==="validate"?"Document validé.":"Correction demandée.");await load();}return <main style={{maxWidth:1180,margin:"0 auto",padding:28}}><p style={{fontSize:12,fontWeight:700}}>SELEN DAILY</p><h1>Documents de fin & preuves d’évaluation</h1><p>Contrôle des relevés de présence, certificats de réalisation et copies d’évaluations des acquis déposées lorsque l’évaluation a été réalisée hors Selen.</p><div style={{display:"flex",gap:8,margin:"18px 0"}}><button onClick={()=>setFilter("attention")}>À traiter</button><button onClick={()=>setFilter("all")}>Tous</button></div>{error?<div>{error}</div>:null}{message?<div>{message}</div>:null}{loading?<p>Chargement…</p>:<section style={{display:"grid",gap:12}}>{visible.length===0?<div>Aucun document dans cette vue.</div>:visible.map(doc=><article key={doc.id} style={{border:"1px solid var(--selen-border)",borderRadius:12,padding:16}}><div style={{display:"flex",justifyContent:"space-between",gap:12,flexWrap:"wrap"}}><div><strong>{labels[doc.document_type]??doc.document_type}</strong>{typeof doc.metadata?.learner_name==="string"?` · ${doc.metadata.learner_name}`:""}<div style={{fontSize:12}}>{doc.organisations?.name??"Organisme"} · v{doc.version} · {doc.status}</div></div><div style={{display:"flex",gap:8}}><a href={`/agent/api/daily/posttraining-documents/download?id=${encodeURIComponent(doc.id)}`} target="_blank" rel="noreferrer">Télécharger</a>{doc.status!=="validated"?<><button onClick={()=>void review(doc.id,"validate")}>Valider</button><button onClick={()=>void review(doc.id,"request_correction")}>Correction</button></>:null}</div></div></article>)}</section>}</main>}
